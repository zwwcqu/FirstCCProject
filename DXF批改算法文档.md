# DXF 工程图批改算法文档

## 〇、全部配置参数

所有数值参数均配置在此处，代码中不应有硬编码数值。

### 对齐相关

| 参数名 | 默认值 | 说明 |
|:------:|:------:|------|
| ALIGN_METHOD | `top_left` | 对齐基准点选型。可选：`top_left`（左上角）、`bottom_left`（左下角）、`center`（中心点），扩展时可增加其他策略 |

### 视图分离相关

| 参数名 | 默认值 | 说明 |
|:------:|:------:|------|
| MERGE_MARGIN | 3.0 | mm，包围盒合并间距。两个实体的包围盒间距小于此值视为同一视图 |
| VIEW_SPLIT_RATIO | 0.5 | 上下半区分界比例。取坐标范围中间值，上半区为视图中央偏上部分 |

### 实体匹配相关

| 参数名 | 默认值 | 说明 |
|:------:|:------:|------|
| ENABLE_LINE_MERGE | false | 是否启用重叠线合并。开启后同一图层的重叠 LINE 会被合并为一条，适合装配图批改场景 |
| POINT_TOLERANCE | 0.002 | mm，端点匹配容差。对齐后两条线的对应端点距离小于此值视为相同点 |
| SHORT_LINE_TOLERANCE | 0.005 | mm，短线段端点容差。短线段相对误差更大，单独设置 |
| SHORT_LINE_THRESHOLD | 1.0 | mm，判断为"短线段"的长度阈值。长度低于此值的 LINE 使用 `SHORT_LINE_TOLERANCE` |
| LENGTH_FILTER | 0.005 | mm，长度过滤阈值。两条 LINE 的长度差超过此值直接判定为不匹配，不参与端点对比 |
| RADIUS_TOLERANCE | 0.002 | mm，CIRCLE 和 ARC 的半径匹配容差 |
| CENTER_TOLERANCE | 0.002 | mm，CIRCLE 和 ARC 的圆心距匹配容差 |
| ANGLE_TOLERANCE | 1.0 | 度，ARC 的起止角度匹配容差 |

### 评分相关

| 参数名 | 默认值 | 说明 |
|:------:|:------:|------|
| WEIGHT_VIEW | 0.1 | 总分中视图完整度的权重 |
| WEIGHT_GRAPH | 0.5 | 总分中图形匹配的权重 |
| WEIGHT_DIM | 0.4 | 总分中尺寸匹配的权重 |
| VIEW_WEIGHTS | `{"主视图": 0.4, "右视图": 0.3, "俯视图": 0.3}` | 各视图在图形分中的权重。视图数量变化时自动归一化 |
| VIEW_COMPLETE_SCORES | `{3: 100, 2: 70, 1: 40}` | 视图完整度得分表。键为标准视图数，值为各学生视图数对应的分数 |
| GRADE_THRESHOLDS | `{90: "优秀 ✅", 70: "良好 ⚠️", 50: "及格 ❌"}` | 评分等级阈值。分数 >= 90 为优秀，>= 70 为良好，>= 50 为及格，< 50 为不及格 🔴 |

---

## 一、整体架构

批改分为两个独立阶段：

```
┌──────────────────────────────────────┐
│     第一阶段：提取参考图信息（一次性）         │
│                                        │
│  输入：标准图.dxf                       │
│  输出：参考图信息.json（可复用）              │
└──────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────┐
│     第二阶段：评改学生图（批量，每张图一次）    │
│                                        │
│  输入：学生图.dxf + 参考图信息.json         │
│  输出：评分报告                            │
└──────────────────────────────────────┘
```

这样拆分的好处：
- 参考图只需处理一次，反复使用
- 批量批改时不必重复提取参考图信息
- 参考图信息可版本管理（老师修改标准答案后重新生成一次即可）

---

## 二、数据格式

### 2.1 归一化实体（无方向格式）

所有轮廓线实体在提取时转为**无方向表示**，消除原始 DXF 中的起点/终点顺序差异。

**LINE：**  
存储为 `(端点A, 端点B, 长度)`。  
两个端点按坐标排序（先比较 x，再比较 y），保证顺序固定。  
无论 DXF 中 LINE 绘制的方向如何，存储后均为同一表示。

**ARC：**  
存储为 `(圆心, 半径, 起角, 止角, 弧长)`。  
如果起角大于止角（弧跨过 0°），则将止角 +360° 归一化（例如 350°→20° 转为 350°→380°），保证起角始终小于止角。

**CIRCLE：**  
存储为 `(圆心, 半径, 周长)`。圆本身没有方向概念。

### 2.2 参考图信息 JSON 结构

```json
{
  "version": "1.0",
  "source_file": "48 壳体三视图.dxf",
  "基准点": {
    "x": 49.3,
    "y": 270.7
  },
  "视图分组": [
    {
      "name": "主视图",
      "实体": {
        "LINE": [
          {"p1": {"x": 55.8, "y": 214.2}, "p2": {"x": 55.8, "y": 265.2}, "长度": 51.0}
        ],
        "CIRCLE": [],
        "ARC": [
          {"center": {"x": 49.3, "y": 239.7}, "radius": 25.0, "起角": 343.7, "止角": 376.3, "弧长": 14.19}
        ]
      },
      "实体统计": {"LINE": 24, "CIRCLE": 0, "ARC": 2}
    },
    {
      "name": "右视图",
      "实体": {
        "LINE": [...],
        "CIRCLE": [
          {"center": {"x": 277.8, "y": 239.7}, "radius": 25.5, "周长": 160.22}
        ],
        "ARC": []
      },
      "实体统计": {"LINE": 12, "CIRCLE": 6, "ARC": 0}
    },
    {
      "name": "俯视图",
      "实体": {
        "LINE": [...],
        "CIRCLE": [...],
        "ARC": [...]
      },
      "实体统计": {"LINE": 9, "CIRCLE": 5, "ARC": 3}
    }
  ],
  "尺寸列表": [5, 8, 12, 14, 25, 28, 30, 38, 40, 44, 49, 51, 56, 71, 74, 240],
  "尺寸总数": 16,
  "实体总数": 61
}
```

说明：尺寸列表去重后为 16 个（因 5、12、25 各出现两次，去重后仅保留一个）。

---

## 三、第一阶段：提取参考图信息

### 输入
标准答案 DXF 文件 + 配置参数（使用默认值或自定义）。

### 输出
参考图信息 JSON 文件。

### 流程

#### 步骤 1：自对齐——找到基准点

```
判断 ALIGN_METHOD 的取值：

top_left（默认）：
  收集所有轮廓线端点（LINE 两端点、CIRCLE 和 ARC 的圆心）的坐标
  基准点 = (所有x的最小值, 所有y的最大值)

bottom_left：
  基准点 = (所有x的最小值, 所有y的最小值)

center：
  基准点 = ((所有x的最小值+最大值)/2, (所有y的最小值+最大值)/2)

这个基准点将被保存到 JSON 中，用于第二阶段对齐学生图。
```

#### 步骤 2：保存对齐后的参考图

将原始 DXF 另存一份，文件名为 `原文件名_对齐.dxf`。  
这一步主要是为了：如果学生图对齐后有疑问，可以打开参考图的对齐版本来对照。

#### 步骤 3：提取轮廓线

过滤条件：

| 排除项 | 条件 |
|--------|------|
| 中心线 | 图层含"点划线"或"中心线" |
| 标注 | dxftype 为 DIMENSION |
| 剖面线 | dxftype 为 HATCH |

保留所有 LINE、CIRCLE、ARC 实体，转为**归一化格式**。

**可选处理：重叠线合并**

> 此步骤为可选，默认关闭。通过配置参数 `ENABLE_LINE_MERGE = true` 开启。

当多个线段（LINE 类型）来自不同图层时，可能代表不同零件或不同性质的轮廓。仅当两条 LINE 同时满足以下所有条件时，才合并为一条线段：

1. 图层相同
2. 方向平行（叉积 < 容差）
3. 投影区间有交集（两个线段在方向轴上的投影区间有重叠）

合并方式：取四个端点中在方向轴上投影值最小和最大的两个端点作为合并后的端点。合并后丢弃中间的所有端点。

```
算法：

对同一图层的 LINE 两两比较：
  if 方向平行：
    将四个端点投影到方向向量上，得到一维标量值 t1...t4
    区间 A = (min(t1, t2), max(t1, t2))
    区间 B = (min(t3, t4), max(t3, t4))
    if 区间A和区间B有交集（A.max > B.min 且 B.max > A.min）：
      → 合并为一条 LINE，取最远两个端点
      → 标记双方已用
```

注意：不同图层的 LINE 即使重叠也不合并，避免混淆零件边界。

#### 步骤 4：视图分离——包围盒重叠合并

**参与包围盒计算的实体：**

| 实体类型 | 参与 | 原因 |
|:--------:|:----:|------|
| LINE（轮廓线层） | ✅ | 构成图形本身 |
| CIRCLE（轮廓线层） | ✅ | 构成图形本身 |
| ARC（轮廓线层） | ✅ | 构成图形本身 |
| LINE（点划线/中心线层） | ❌ | 中心线延伸到视图之间，会串组 |
| CIRCLE/ARC（点划线层） | ❌ | 分度圆可能跨视图边界 |
| DIMENSION | ❌ | 尺寸标注在轮廓外侧，会扩大包围盒吞并相邻视图 |
| HATCH | ❌ | 剖面线在图形内部不影响边界 |

每个参与实体按如下方式生成包围盒：

```
LINE:         包围盒 = (start.x, start.y, end.x, end.y)
CIRCLE:       包围盒 = (center.x-r, center.y-r, center.x+r, center.y+r)
ARC:          包围盒 = (center.x-r, center.y-r, center.x+r, center.y+r)
```

合并规则：

```
1. 每个轮廓线实体生成一个矩形包围盒
2. 包围盒之间有重叠（间距 < MERGE_MARGIN）的两个实体，归为同一组
3. 用并查集（Union-Find）迭代合并，直到没有新的合并发生
4. 每组就是一个独立的视图
```

为什么不用端点相连法？因为圆和弧没有显式的端点，无法通过端点连接来判断归属。包围盒法对所有实体类型一视同仁。

合并完成后，按各组包围盒的位置关系判断视图名称：

| 位置条件 | 视图名 |
|----------|:------:|
| 在上半区（y > 总y范围中间值）且偏左 | 主视图 |
| 在上半区且偏右 | 右视图 |
| 在下半区（y < 总y范围中间值） | 俯视图 |

如果只有一个视图组，不命名，直接作为"当前视图"。

#### 步骤 5：提取尺寸信息

```
从所有 DIMENSION 实体中提取测量值（get_measurement()），去重后存入 JSON。
直径标注保留原始数值（不含 Φ 前缀）。

去重说明：如果同一数值出现多次（如5、25各出现两次），只保留一个。
```

#### 步骤 6：导出 JSON

将基准点、视图分组（含所有归一化实体）、尺寸列表写入 JSON 文件，文件名 `参考图信息.json`。

---

## 四、第二阶段：评改学生图

### 输入
学生作业 DXF 文件 + 参考图信息 JSON + 配置参数。

### 输出
评分报告（文本格式）。

### 流程

#### 步骤 1：对齐——用参考图的基准点平移

```
读取参考图 JSON 中的基准点 (ref_x, ref_y)

提取学生图的基准点（方法同第一阶段步骤1，使用相同的 ALIGN_METHOD）：

偏移量 = 参考基准点 - 学生基准点
学生图所有坐标 += 偏移量

此时学生图与参考图已处于同一坐标系。
```

#### 步骤 2：保存对齐后的学生图

另存为 `学生文件名_对齐.dxf`，供人工复核用。

#### 步骤 3：提取学生图信息

重复第一阶段步骤 3~5：
- 过滤轮廓线、转为归一化格式
- 视图分离（包围盒合并，使用 MERGE_MARGIN）
- 提取尺寸列表

#### 步骤 4：逐视图匹配

**LINE 匹配：**

```
对参考视图中每条 LINE：
  从学生视图中找未匹配的 LINE：
    过滤条件：长度差 > LENGTH_FILTER → 跳过
    匹配条件（使用 POINT_TOLERANCE 或 SHORT_LINE_TOLERANCE）：
      如果线段长度 < SHORT_LINE_THRESHOLD：
        容差 = SHORT_LINE_TOLERANCE
      否则：
        容差 = POINT_TOLERANCE
      
      参考线端点A ≈ 学生线端点A
      且 参考线端点B ≈ 学生线端点B
    → 匹配成功，标记双方已用
```

**CIRCLE 匹配：**

```
对参考视图中每个圆：
  从学生视图中找未匹配的圆：
    半径差 > RADIUS_TOLERANCE → 跳过
    圆心距 > CENTER_TOLERANCE → 跳过
    → 匹配成功
```

**ARC 匹配：**

```
对参考视图中每条弧：
  从学生视图中找未匹配的弧：
    半径差 > RADIUS_TOLERANCE → 跳过
    圆心距 > CENTER_TOLERANCE → 跳过
    起角差 > ANGLE_TOLERANCE → 跳过
    止角差 > ANGLE_TOLERANCE → 跳过
    → 匹配成功
```

**DIMENSION 匹配（尺寸）：**

```
参考尺寸集合（去重）与 学生尺寸集合（去重）取交集。
交集 = 正确尺寸
仅参考图有 = 漏标尺寸
仅学生图有 = 多标尺寸
```

尺寸匹配不受位置偏移影响。

---

## 五、评分公式

### 5.1 视图完整度（权重 = WEIGHT_VIEW，默认 0.1）

```
根据 VIEW_COMPLETE_SCORES 查表得分。

默认表：
  参考3个视图，学生3个 → 100分
  参考3个视图，学生2个 → 70分
  参考3个视图，学生1个 → 40分
  参考2个视图，学生2个 → 100分
  参考2个视图，学生1个 → 60分
  参考1个视图，学生1个 → 100分

如果学生视图数多于参考视图数 → 100分（多画不扣分）
```

### 5.2 图形匹配分（权重 = WEIGHT_GRAPH，默认 0.5）

```
每个视图单独计算：
  视图图形分 = 该视图匹配实体数 / 该参考视图总实体数 × 100

如果学生视图数少于参考视图数，缺失视图按 0 分计算。

图形总分 = Σ(各视图图形分 × 对应视图权重)

视图权重由 VIEW_WEIGHTS 配置：
  默认：主视图 0.4，右视图 0.3，俯视图 0.3

如果视图数量与默认配置不同（如只有1个视图）：
  视图权重自动归一为 1.0（即 100%）
```

### 5.3 尺寸匹配分（权重 = WEIGHT_DIM，默认 0.4）

```
尺寸分 = 正确尺寸数 / 参考尺寸总数 × 100
```

### 5.4 总分

```
总分 = 视图完整度×WEIGHT_VIEW + 图形匹配分×WEIGHT_GRAPH + 尺寸匹配分×WEIGHT_DIM
```

### 5.5 评分等级

```
根据 GRADE_THRESHOLDS 判定等级：
  >= 90 → 优秀 ✅
  >= 70 → 良好 ⚠️
  >= 50 → 及格 ❌
  < 50  → 不及格 🔴
```

---

## 六、输出报告格式

```
─────────────────────────────────
       DXF 工程图批改报告
─────────────────────────────────
参考图: 48 壳体三视图.dxf
学生图: 张三_作业.dxf

📐 视图完整度: 3/3 = 100分 ✅

📐 图形匹配
  主视图: 24/26 = 92分   漏2条线
  右视图: 18/18 = 100分  全部正确
  俯视图: 15/17 = 88分   漏2条线
  图形总分: 93分

📏 尺寸匹配
  正确: 16个  漏标: 0个  多标: 2个(5.0,93.5)
  尺寸分: 100分

━━━━━━━━━━━━━━━━━━━━━━━
  总得分: 95分  等级: 优秀 ✅
━━━━━━━━━━━━━━━━━━━━━━━

❌ 错误详情:
  主视图:
    - 漏 LINE (55.8,214.2)→(55.8,265.2) 长度51.0
  俯视图:
    - 漏 LINE (122.3,246.7)→(126.8,246.7) 长度4.5
    - 漏 LINE (126.8,246.7)→(126.8,232.7) 长度14.0
  多标尺寸: 5.0, 93.5
```

> 注意：所有坐标提取时以万分位四舍五入（保留到千分位），与容差值一致。

## 七、已知限制

1. **旋转变换不支持：** 如果学生图旋转了（不是平移），简单平移对齐会失效
2. **缩放不支持：** 学生图如果缩放了，长度过滤会错误排除所有线段
3. **块（BLOCK）未展开：** 如果学生作业用了块引用，需先展开为基本实体
4. **3D 投影图重叠线：** 3D 模型投影的 DXF 有多条重叠线，会降低匹配准确率
5. **手动绘图端点不精确对齐：** 学生手画时端点未精确对齐，可能导致包围盒分组合并出现偏差
6. **剖面线仅判断有无：** 不校验剖面线面积和图案是否正确

---

## 八、使用建议

- **适用场景：** 手工绘制的 2D 机械制图作业批改（有 DXF 源文件）
- **不适用场景：** 3D 模型自动投影生成的工程图（线条重叠严重，建议改用 AutoCAD Compare）
- **最佳实践：** 尺寸匹配（默认 40%）作为核心分，图形匹配（默认 50%）作为辅助验证
- **容差建议：** 严格图纸 POINT_TOLERANCE=0.2mm，普通练习 POINT_TOLERANCE=0.5mm
- **配置继承：** 同一题库的批改使用同一套配置参数，可通过参数模板快速切换严格/宽松模式
